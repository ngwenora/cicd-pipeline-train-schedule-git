# kind: Service
# apiVersion: v1
# metadata:
#   name: train-schedule-service
# spec:
#   type: NodePort
#   selector:
#     app: train-schedule
#   ports:
#   - protocol: TCP
#     port: 8080
#     nodePort: 30080


apiVersion: v1
kind: Service
metadata:
  name: train-schedule-loadbalancer
  namespace: staging
spec:
  type: LoadBalancer
  selector:
    app: train-schedule
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080 
